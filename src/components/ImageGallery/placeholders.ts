// Единая моковая заглушка — фото жилого дома из Unsplash, скачанное
// локально в /public/placeholder.jpg. Используется галереей, когда
// у объекта нет настоящих фото, чтобы карточка не была пустой и
// можно было показать саму механику миниатюр/свайпа.

export type MockImage = { image: { id: string; url: string; alt: string } }

// 4 «кадра» — все указывают на один файл; этого достаточно для
// демонстрации стрипа миниатюр и переключения, и при этом
// сохраняется быстрая загрузка (один реальный байт-сет).
export const MOCK_GALLERY: MockImage[] = [1, 2, 3, 4].map((i) => ({
  image: {
    id: `mock-${i}`,
    url: '/placeholder.jpg',
    alt: `Фото объекта ${i}`,
  },
}))
